ci:
  collect:
    settings:
      chromeFlags: --ignore-certificate-errors

  assert:
    preset: lighthouse:lr-desktop

  upload:
    # upload options here

  server:
    # server options here

  wizard:
    # wizard options here
