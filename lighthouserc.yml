ci:
  collect:
    settings:
      chromeFlags: --disable-gpu --ignore-certificate-errors
      maxWaitForFcp: 15 * 1000
      maxWaitForLoad: 35 * 1000
      emulatedFormFactor: desktop
      throttling: desktopDense4G

  assert:
    assertions:
      categories:performance:
        - error
        - minScore: 0.9
      categories:accessibility:
        - error
        - minScore: 0.9

  upload:
    # upload options here

  server:
    # server options here

  wizard:
    # wizard options here
