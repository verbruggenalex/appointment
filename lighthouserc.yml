ci:
  collect:
    settings:
      chromeFlags: "--disable-gpu --ignore-certificate-errors"

  assert:
    assertions:
      categories:performance:
        - error
        - minScore: 0.9
      categories:accessibility:
        - error
        - minScore: 0.87

  upload:
    # upload options here

  server:
    # server options here

  wizard:
    # wizard options here
