http:
  routers:
    traefik:
      rule: "Host(`traefik.{{env "TRAEFIK_DOMAIN"}}`)"
      service: "api@internal"
      tls:
        domains:
          - main: "{{env "TRAEFIK_DOMAIN"}}"
            sans:
              - "*.{{env "TRAEFIK_DOMAIN"}}"
  middlewares:
    secure-basicauth:
      basicauth:
        users: "lex:$apr1$Nq5v/Dh3$z.PrrgbmfP.fABwykzzLG1"
#        users: "{{env "TRAEFIK_BASIC_AUTH"}}"
  #   secure-whitelist:
  #     ipwhitelist:
  #       sourcerange:
  #          - 127.0.0.1/32
  #          - 172.29.0.1/32

tls:
  certificates:
    - certFile: "/etc/certs/local-cert.pem"
      keyFile: "/etc/certs/local-key.pem"
